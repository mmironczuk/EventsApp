@model IEnumerable<EventsApp.Models.Favourites>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AccountId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MainEvent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AccountId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MainEvent.MainEventId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FavouritesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FavouritesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FavouritesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
